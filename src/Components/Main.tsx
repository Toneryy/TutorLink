import React from 'react';
import m from '../Styles/App.module.scss';


const Main: React.FC = () => {
    return(
        <main className={m.main}>
            sa
        </main>
    );
};

export default Main;